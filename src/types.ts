export type CompanyType = {
  id: number;
  name: string;
  slug: string;
  timezone: string;
};

export type CheckInType = {
  id: number;
  start: string;
};

export type TeamType = {
  id: number;
  name: string;
};
